export interface Appointment {
    id?: string,
    date: string,
    done: boolean,
    name: string,
    hour: string,
    price?: string,
    material?: string,
    note: string,
    service: string
}